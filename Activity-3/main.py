# Python program to calculate the sum of the first ten natural numbers
# Use bulit-in sum and range functions to caluculate the sum 
total_sum = sum(range (1, 11))
print(f"The sum of the first ten nautral numbers is {total_sum}")